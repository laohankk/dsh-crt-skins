//#region src/index.ts
/** Cordis row name, matching the `id` used in cordis.patch.yml. */
const name = "crt-terminal";
/** Node half is intentionally inert: this skin is browser-only styling. */
function apply() {}
//#endregion
export { apply, name };
